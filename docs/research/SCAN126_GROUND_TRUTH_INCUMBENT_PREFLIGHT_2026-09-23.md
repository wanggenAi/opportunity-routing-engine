# Scan 126 F1 — Exact Incumbent / Control-Surface Preflight

Date: 2026-09-23

Live TypeSafe/Jev on PR #431 actually evaluated `ATTRACTION_SCAN_126-F1` and returned:

```text
input_scan_id = ATTRACTION_SCAN_126
entity_count = 1
effective route = EXACT_INCUMBENT_PREFLIGHT
continuation = EXECUTE_RESEARCH_QUEUE
```

This document consumes that route.

## Result

`ATTRACTION_SCAN_126-F1 — CROSS_BORDER_CHINA_GROUND_TRUTH_EXECUTION_ROUTING` is **demoted**.

The field is real, but the broad control edge is not.

### ChinaLocalAgent

Current positioning already covers overseas clients that need one-time/ad-hoc China-local execution. It scopes objective, location, timing and evidence needs, sends local execution, and returns photos/videos/documents/checklists/reports where permitted. This is almost the same service boundary as the broad F1.

### Upwork

Upwork already owns:
- self-revealing global buyer demand;
- direct matching to China-based talent;
- funded fixed-price milestones before work starts;
- contract/dispute/payment rails;
- transparent agency / client-consented delegation paths.

A new operator cannot count those rails as its own moat.

### WeiChaiShi

WeiChaiShi already owns:
- a large China local crowd;
- nationwide retail audit and field data collection;
- task preparation/publication;
- real-time submissions;
- quality review;
- approval/payment.

This directly absorbs the replaceable-executor + evidence-QA layer for the strongest early task class (retail/store evidence).

### Premise

Premise proves the generalized mechanism is mature globally: remote organizations configure local place/route tasks; contributors capture local photos/geolocation; the platform validates submissions and delivers structured data through dashboards/data interfaces.

### UU Paotui

UU does not prove evidence-specific field verification, but its 2026 MCP/CLI/Agent Skill/OpenAPI surface shows that machine-callable China-local physical execution is itself becoming a native platform primitive.

## Why narrowing does not rescue F1

A narrower concept could be described as:

```text
machine-native cross-border scope normalization
+ multi-rail local dispatch
+ evidence acceptance
```

But no distinct operator asset is evidenced:
- no exclusive buyer-intent rail;
- no exclusive executor supply;
- no exclusive action right;
- no proprietary outcome graph yet;
- no control right incumbents cannot reproduce;
- generic agents can already help compose marketplaces, forms, payment and task APIs.

That makes the remaining layer a thin integration / coordination feature vulnerable to generic-agent substitution and incumbent absorption.

## Commercial truth

Paid demand remains useful reality evidence. It is **not** a reason to launch a generic China helper, sourcing agency or field-task broker.

```text
REAL FIELD
+ REAL MONEY
+ REAL SUPPLY
!= UNOWNED CONTROL EDGE
```

No bootstrap is authorized for F1.

- commercial promotion: false
- retained research formation: false
- FIRST_EXTERNAL_VALUE_FLOW: NOT_PROVEN

## Next boundary

Advance to Scan 127 and return to formation-diverse broad reality using the success/failure calibration only as a search prior.

Do not recycle ground-truth execution, sourcing, local-helper, retail-audit or field-task dispatch as a fallback mechanism.
