# TASK_STATE.md

Operational handoff only. GitHub live state wins if anything below conflicts with repository facts.

## Current Mission
The active commercial portfolio has been explicitly reset to a clean slate. All pre-reset cases are historical only and must not seed active discovery. The current mission is to find directly routable paid value flows where two real sides already exist and one connection can create an accepted result and money without first building a platform, acquiring an unrelated identity or entering a long institutional process.

## Goal
Preserve the constitutional cognition:

```text
REALITY > COGNITION > SCHEMA
```

and restore the commercial path:

```text
REAL PAIN / UNUSED CAPACITY
→ REAL COUNTERPARTY PAID NEED
→ BOUNDED TASK
→ MATCH
→ DELIVERY
→ ACCEPTANCE
→ MONEY
→ REPEAT
```

The operator should be the connector / organizer / acceptance layer, not the permanent delivery labor.

## Current Unique Commercial Research Goal
Validate fresh value flows only.

For each fresh flow, bind:
- one current buyer-side task;
- buyer budget or current payment signal;
- one independent provider-side availability/quote signal;
- bounded acceptance criteria;
- a simple settlement path;
- a reason the operator remains useful beyond introduction.

No pre-reset candidate may be revived without fresh post-reset evidence.

## Current Phase
`CLEAN_SLATE_RESET_MERGED / MAIN_GREEN / PRE_RESET_CASES_INACTIVE / TRANSACTION_UNIT_VALIDATION_NEXT`

## Last Verified Main
`71c8f55e36b01465ef985cc78f8dc5cebb2685c3` — PR #238 squash-merged. Main CI run #740 (run id `35422252170`) passed on this exact SHA. The state-only checkpoint commit that updates this file may advance live `main` with `[skip ci]`; this SHA remains the latest functional/CI-verified main before that checkpoint write.

## Active Issue
#143 — `P0: First external value flow — field proof, not more architecture`.

Clean-slate reset was synced to Issue #143 in comment id `5739446249`.

The issue remains the mission anchor only. Its older route-specific comments are historical context and are not active candidate instructions after this reset.

## Active Branch
None. `feature/clean-slate-value-flow-001` is merged and is not an active source of truth.

## Active PR
None. PR #238 (`Reset active portfolio and start clean-slate value-flow scan`) is merged.

## CI
- PR #238 final head: `ab7eb58412461c9f05dce38049b2151e45df3395`
- PR CI: run #739, run id `35422237679`, success
- merge SHA: `71c8f55e36b01465ef985cc78f8dc5cebb2685c3`
- main CI: run #740, run id `35422252170`, success

## Latest Artifact / Persisted State
Active reset:
- `docs/research/CLEAN_SLATE_COMMERCIAL_RESET_2026-09-19.md`

Fresh scan:
- `data/clean_slate_value_flow_scan_001_2026-09-19.json`
- `field/clean-slate-value-flow-scan-001.md`

Historical only after reset:
- all `opportunity_mobilization_scan_001..028` candidate/route conclusions;
- all pre-reset connection-pressure / residual candidate / technical-manager route cases;
- all earlier candidate rankings and active-route declarations.

Historical files remain in Git for audit/regression but are not active priors.

## Completed
- Explicitly reset the active commercial portfolio to zero.
- Preserved old research only as historical evidence and regression material.
- Restored the direct value-flow commercial logic: two real sides, bounded task, acceptance, settlement and visible money path.
- Explicitly removed identity-first, subsidy-first, marketplace-first and long-institutional-process routes from current active discovery.
- Executed a fresh public-web scan without using prior cases as candidate seeds.
- Retained three fresh transaction shapes:
  - `CSVF-001` local commercial content micro-orders;
  - `CSVF-002` SME AI / office-workflow micro-project routing;
  - `CSVF-003` local-presence field microtasks.
- Found current evidence that commercial content is already bought per video/per shoot with explicit acceptance.
- Found current government and enterprise evidence that SMEs lack AI talent and already use flexible/project-based AI implementation.
- Found a current direct buyer example of a foreign company offering about RMB 700-800 for one day of checklist-driven exhibition field research by a local student/part-timer.
- Sent no outreach and made no formal opportunity promotion.
- PR #238 and main CI are green.

## Current Findings
- The cleanest commercial signal is a task that already has a buyer, bounded output, acceptance condition and current payment.
- `CSVF-003` is a strong structural illustration because physical local presence is scarce to the remote buyer but abundant to local students/young residents; however repeatability and durable operator value are still unknown.
- `CSVF-001` already has explicit per-unit acceptance and payment mechanics, but bypass risk is high unless the operator owns recurring intake, quality consistency or replacement.
- `CSVF-002` has real enterprise demand and routable talent, but must be killed if it collapses into founder consulting/custom development/support.
- None of the three is yet a formal opportunity.
- The technical-manager route and all other pre-reset examples are inactive regardless of prior research depth.

## Blockers
No fresh flow yet has a complete transaction equation:

```text
buyer task + buyer budget
+
provider availability + provider quote
+
acceptance
+
operator margin
```

The current blocker is transaction-unit truth, not architecture.

`FIRST_EXTERNAL_VALUE_FLOW = NOT_PROVEN`.

## Next Action
Validate `CSVF-001`, `CSVF-002` and `CSVF-003` only at transaction-unit level using fresh evidence. For each, bind one current buyer-side task/budget and one independent provider-side availability/quote signal. Kill any flow that cannot produce a simple buyer-task-provider-margin equation without building infrastructure first.

Do not send outreach until a specific fresh transaction unit has been selected and the user explicitly authorizes external contact.

## Do Not Repeat
- Do not reactivate any pre-reset case because prior work was invested in it.
- Do not continue the technical-manager rail.
- Do not start from qualification, policy, subsidy, platform or identity.
- Do not build a marketplace before one transaction.
- Do not use vague “consulting” as the deliverable.
- Do not let the operator become permanent delivery labor.
- Do not call flexible labor supply a business unless a current paying task is also bound.
- Do not call buyer demand an opportunity unless provider capacity and acceptance are also bound.
- Do not hide a missing money path behind ecosystem language.
- Do not force the fresh flows into one score before their transaction units are evidenced.

## Guardrails
- GitHub live state is the operational source of truth.
- `REALITY > COGNITION > SCHEMA`.
- `HISTORICAL CASE != ACTIVE CANDIDATE`.
- `PAST RESEARCH INVESTMENT != CURRENT PRIORITY`.
- `MONEY PATH BEFORE ARCHITECTURE`.
- `BUYER NEED + PROVIDER CAPACITY != VALUE FLOW UNTIL ACCEPTANCE AND SETTLEMENT`.
- `INTRODUCTION != DURABLE ORCHESTRATION VALUE`.
- `UNKNOWN != PASS`.
- Infer broadly/boldly for discovery; promote conservatively.
