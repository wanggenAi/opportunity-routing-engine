# Tests

Automated tests will be introduced with production code.

Priority test areas:

- deterministic qualification gates;
- `UNKNOWN` not treated as `PASS`;
- contactability hard gate;
- deduplication;
- evidence/provenance preservation;
- scoring boundaries;
- opportunity lifecycle transitions;
- supplier-match constraints;
- regression tests for prompt-assisted structured outputs where feasible.
